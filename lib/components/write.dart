import 'package:flutter/material.dart';

class WritePage extends StatelessWidget {
  static const id = 'write_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
            "독후감 쓰기"
        ),
      ),
      body: Text("asdf"),
    );
  }
}
